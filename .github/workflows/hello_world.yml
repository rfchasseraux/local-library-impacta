name: Hello World

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  job1:
    name: Primeiro Job
    runs-on: ubuntu-latest

    steps:
      - name: Passo 1
        run: env | sort
  
  job2:
    name: Segundo Job
    runs-on: windows-latest
    needs: job1
    steps:
      - name: Passo 1
        run: "Get-ChildItem Env: | Sort-Object Name"
